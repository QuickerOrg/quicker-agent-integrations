# 兼容性与验证状态

本仓库按「实现、传输验证、平台安装、真实 Quicker 验收」分别记录状态。安装包能被平台识别，不等于真实动作编写已经验收。

## 当前状态

| 项目 | 状态 |
| --- | --- |
| Codex 插件结构与技能 | 已实现；v0.1.1 包通过 Codex plugin-creator 校验 |
| Codex GitHub 市场安装 | 2026-09-07：从公开仓库安装，Codex 确认 installed=true、enabled=true |
| Codex 原生缓存启动与工具发现 | 2026-09-07：Codex CLI 0.153.0；完整包经隔离市场安装，app-server 从缓存启动转接并发现 fixture 工具；未调用模型或创建任务 |
| PowerShell 5.1 传输 | 2026-09-07：独立仓库 16 项契约测试全部通过，包含实际启动配置、中文与空格安装目录 |
| 真实 Quicker 创建、保存和预览 | 尚待启用本机 MCP 后验收 |
| Quicker Debug | 提供当前 MCP 接口，需手动启用和授权 |
| Quicker Release | 当前没有 MCP 入口，不监听 |
| Cursor 与其他 Agent | 计划接入，尚未实现和验收 |

## 版本记录

每个平台使用自己的插件 manifest 版本。发布记录应包含平台版本、Quicker 版本或构建、操作系统、插件版本与实测结果。尚未实测的最低版本写为未知，不猜测兼容范围。

当前 Codex 包版本为 `0.1.1`，通过 GitHub 市场分发。旧个人市场原型使用的 `+codex.<时间戳>` 后缀只用于刷新开发缓存，不代表跨平台发布版本。

`0.1.1` 修复 `0.1.0` 安装成功但 MCP 无法启动的问题：当前 Codex legacy MCP 加载器不会展开参数中的 `${PLUGIN_ROOT}`，现改用 `cwd: "."` 与包内相对脚本路径。旧版用户按 README 的市场升级与重新安装步骤更新。原生启动验证使用隔离 HTTP fixture，真实 Quicker 动作验收仍单独记录。

## 真实验收

使用用户授权的测试环境与一个容易识别的动作标题，依次验证：

1. 从平台发现 Quicker 工具，读取运行时 action-source 技能。
2. 创建草稿，记录 actionId 和 slot，写入只显示提示的程序。
3. 保存成功后读回内容，并在 Quicker 暂存区核对动作。
4. 按用户要求打开设计器预览。需要验证运行时，先确认用户允许执行该动作。
5. 单独验证只读模式和客户端拒绝访问的行为，记录现象。

只记录结果、版本及非敏感错误码。配置文件、token、真实动作库和客户端同意记录不作为测试附件上传。
