# 兼容性与验证状态

本仓库按「实现、传输验证、平台安装、真实 Quicker 验收」分别记录状态。安装包能被平台识别，不等于真实动作编写已经验收。

## 当前状态

| 项目 | 状态 |
| --- | --- |
| Codex 插件结构与技能 | 已实现；v0.1.1 包通过 Codex plugin-creator 校验 |
| Codex GitHub 市场安装 | 2026-09-07：从公开仓库安装 v0.1.1，真实用户配置下的 Codex app-server 确认 installed=true、enabled=true |
| Codex 原生缓存启动与工具发现 | 2026-09-07：Windows、Codex CLI 0.153.0；按实际安装缓存的 MCP 配置启动 PowerShell 5.1 转接，连接真实 Quicker，发现 18 个工具及运行时技能 |
| PowerShell 5.1 传输 | 2026-09-07：独立仓库 16 项契约测试全部通过，包含实际启动配置、中文与空格安装目录 |
| 真实 Quicker 创建、保存和预览（Debug） | 2026-09-07：加载 action-source、创建、写入、保存与读回核对通过；浏览器设计器显示对应草稿标题和提示消息步骤。未执行动作 |
| 真实只读切换与拒绝访问 | 本次未验证；隔离传输测试不能替代真实权限验收 |
| Quicker Debug | 本次验收依赖包含默认技能包发现修复的本地构建，该修复尚未公开发布；需启用 MCP 和允许写入并完成客户端授权 |
| Quicker Release | 支持已实现，待包含 MCP 新入口与默认技能包发现修复的正式版发布；已发布旧版没有 MCP 入口时仍不可用 |
| MCP 内核测试 | 2026-09-07：Debug 41/41 通过；Release 配置 41 项覆盖通过（40 项全量通过，启停新增用例修正后单项复验通过） |
| 正式前端构建 | 2026-09-07：生产前端构建与产物审计通过 |
| 正式安装包端到端 | 尚未执行正式 MSI / 混淆 Release 的完整验收；不将内核测试或 Debug 草稿验收视为正式安装包已验证 |
| Cursor CLI | 2026-09-07：2026.08.11-e8db854；v0.2.0 本地安装包技能加载、实时知识发现、4 步动作创建/保存/预览通过。IDE 3.7.12 的插件设置界面重载尚未单独操作验收 |
| Claude Code | 2026-09-07：2.1.263；v0.2.0 插件与市场通过官方 validator，隔离安装及真实用户配置下的公开市场安装成功；识别 write-action 技能与 MCP，mcp list 连接真实 Quicker 成功。模型调用因未登录退出，未验证 Claude 模型写动作 |
| VS Code / Gemini CLI | v0.2.0 默认用户配置安装/更新/卸载和隔离传输通过；尚未完成实际客户端写动作验收 |

## 版本记录

每个平台使用自己的插件 manifest 版本。发布记录应包含平台版本、Quicker 版本或构建、操作系统、插件版本与实测结果。尚未实测的最低版本写为未知，不猜测兼容范围。

当前 Claude Code 安装包版本为 `0.2.1`，补齐安装、认证、诊断与共存指引，MCP 运行配置和共享转接与 `0.2.0` 相同；其他安装包仍为 `0.2.0`。各平台分发方式见[客户端安装](客户端安装.md)。旧个人市场原型使用的 `+codex.<时间戳>` 后缀只用于刷新开发缓存，不代表跨平台发布版本。

`0.1.1` 修复 `0.1.0` 安装成功但 MCP 无法启动的问题：当前 Codex legacy MCP 加载器不会展开参数中的 `${PLUGIN_ROOT}`，现改用 `cwd: "."` 与包内相对脚本路径。旧版用户按 README 的市场升级与重新安装步骤更新。隔离 HTTP fixture 用于传输回归，真实 Quicker 验收证据见下。

Release 支持与默认技能包发现修复已实现，待包含这些变更的正式版发布。受支持的新构建在设置 → Agent 中提供「启用 MCP」入口，Debug / Release 均默认关闭；用户要编写动作，还需开启「允许 MCP 写入」并完成客户端同意，loopback、Bearer 与工具审批边界不变。支持 MCP 的 Release 构建在 Quicker 内嵌设计器中预览，不返回 HTTP URL；只有 Debug + Vite 时才可能返回浏览器 URL。

上述产品变更尚未正式发布，已发布旧版没有 MCP 入口时仍不可用。插件 0.1.1 的安装成功不表示产品已具备 MCP 能力；Release 配置内核测试已通过，最低适用正式版本与正式安装包端到端结果仍待确认。

## 真实验收

2026-09-07 在 Windows 上，使用已从公开市场安装的 `quicker@quicker-agent-integrations` 0.1.1 完成以下验证：

- Codex app-server 在真实用户配置下识别已安装并启用的插件，未使用隔离的 Codex 配置目录。按安装缓存内 `.mcp.json` 启动 Windows PowerShell 5.1 转接，成功连接真实 Quicker。
- 初始化返回运行时技能索引，发现 18 个工具，`skill_load(action-source)` 成功。
- 创建测试草稿、写入程序，`quicker_save` 返回 `saved=true`；读回规范化 YAML 与动作信息，核对内容一致。
- `quicker_preview` 返回设计器 URL；实际浏览器中的设计器显示对应标题、提示消息步骤及本机暂存提示。

Quicker MCP 服务报告版本为 `2.3.0`，但本次使用包含默认技能包发现修复的本地 Debug 构建，该修复尚未公开发布。上述结果不代表所有 `2.3.0` 或其他 Debug 构建均兼容。动作没有执行，真实拒绝访问及只读切换也未验证。

后续构建复验时，使用用户授权的测试环境与一个容易识别的动作标题，记录具体构建范围，依次验证：

1. 从平台发现 Quicker 工具，读取运行时 action-source 技能。
2. 创建草稿，记录 actionId 和 slot，写入只显示提示的程序。
3. 保存成功后读回内容，并在 Quicker 暂存区核对动作。
4. 按用户要求打开设计器预览。需要验证运行时，先确认用户允许执行该动作。
5. 单独验证只读模式和客户端拒绝访问的行为，记录现象。

只记录结果、版本及非敏感错误码。配置文件、token、真实动作库和客户端同意记录不作为测试附件上传。

## 0.2.0 跨平台验收

共享转接与技能通过 sync-packages.py 生成独立包。保留 Codex 0.1.1 已验证的相对 cwd 方式；Cursor / Claude 使用其实际支持的插件根变量。Codex 0.2.0 已通过插件校验器和原启动方式回归，未将旧客户端安装结果冒充本次更新安装。

Cursor CLI 从安装器生成的 `%USERPROFILE%/.cursor/plugins/local/quicker` 加载插件，读取 write-action skill 与服务端 action-source。生成“Cursor MCP 验收 · 文本摘要”，4 步完成参数文本修剪、字符数、非空行数和消息框摘要；校验保存到暂存区，quicker_preview 返回对应设计器。实际过程先出现 CLI print 模式自动拒绝；在专用测试目录授权需要的 Quicker 工具后成功，Quicker 权限和批准方式未修改。第一次源文本转义不当由 Cursor 读回发现并修正，保存结果 saved=true。动作未运行，因此这里不声称运行时统计已验收。

Quicker 使用包含 MCP 与默认技能包发现修复的本地 Debug 构建，MCP 启用、允许写入、sandbox_confirm。新插件没有伪装已授权客户端；Client 参数分别发出 cursor-quicker-plugin / claude-quicker-plugin。所有原始日志只留本机，不提交个人配置或 token。

Claude Code 初次 npm 包缺少可选原生二进制；安装对应 Windows x64 2.1.263 包后官方校验、安装和 MCP 健康检查成功。首次默认 30 秒连接超时，使用该客户端 MCP_TIMEOUT=180000 复验 Connected；该变量只用于验收进程，没有修改全局权限或客户端配置。首次连接应留意 Quicker 的同意窗口。

第二轮 Cursor CLI 不传 --plugin-dir，仍自动发现安装器写入的本地插件。打开同一暂存动作，修改说明，保存 editVersion=4，读回说明一致并再次预览；actionId 与 slot 保持一致，没有新建或覆盖正式动作。更新后的安装包 `-Check` 返回 enabled/allowWrites/portReachable=true，无 token 内容。

0.2.0 最终 Windows 测试：23 项通过（16 项传输回归 + 7 项包/安装验证），覆盖两种原生插件启动参数、客户端标识、中文/空格用户目录、重复安装/更新/卸载、已有配置保留、外来同名项与本地修改拒绝、JSONC 原文不改写。

## Claude Code 公开安装复验

2026-09-07，Claude Code 2.1.263 在真实用户配置下从公开 GitHub 市场安装 Quicker 0.2.0，插件详情识别 write-action 技能与 quicker MCP。`claude mcp list` 对 `plugin:quicker:quicker` 返回 `Connected`，启动参数使用 `-Client claude`。

按已安装包的 `.mcp.json` 展开 `CLAUDE_PLUGIN_ROOT` 并启动原装转接后，真实 Quicker 初始化报告 `quicker 2.3.0`，发现 18 个工具；`skill_load(action-source)` 与 `steps.resolve('sys:notify')` 返回运行时知识及真实 schema，完整 content 与 attachedRules 保留。此项验证的是已安装包传输，不是 Claude 模型工具调用。

继续通过该已安装包转接完成测试草稿的 `create → write_file → save → preview`。草稿包含循环、条件、变量和表达式；首次使用 YAML flow collections 被 `validate_failed` 拒绝，按返回错误改为 block style 后在同一草稿保存成功并打开预览。未复制草稿或变更权限，未执行动作。这证明安装包可以承载实际草稿编写链路，内容由验收调用提供，不是 Claude 模型生成。

同一环境中，官方 `codex@openai-codex` 1.0.6 已安装，Codex setup 的就绪与认证检查通过。Claude Code 的 `auth status` 返回 `loggedIn: false`，实际 `--print` 请求退出码为 1，返回 `Not logged in`，未产生模型调用。上述安装、连接及转接草稿验收不表示 Claude 模型创建、保存或预览动作已经通过。

0.2.1 在 Windows 上的 23 项测试通过，新增启动回归将 Cursor / Claude 的完整安装包复制到含中文和空格的缓存目录，再从无关工作目录按实际 manifest 参数启动。Claude Code 2.1.263 对根市场和 Claude 插件的严格清单校验通过。这些结果不替代 Claude 的真实模型验收。

本次变更使用官方 `codex-plugin-cc` 1.0.6 调用 Codex 完成只读审查，未发现本次改动引入的明确缺陷；审查同时确认清单、共享副本一致性与 Python 语法检查通过。审查环境未重跑完整测试，23 项测试结果来自本仓库的独立 Windows 验证。
