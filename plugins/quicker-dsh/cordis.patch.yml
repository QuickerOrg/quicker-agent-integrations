# Insert the Quicker bundle. index.js resolves the stdio relay path and
# registers @deepseek-ai/dsh-mcp-client through ctx.loader.create so the
# token never enters this file.
- insert:
    - id: quicker
      name: dsh-plugin-quicker
